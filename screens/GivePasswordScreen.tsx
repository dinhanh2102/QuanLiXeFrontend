import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const GivePasswordScreen = ({navigation}) => {
  return (
    <View>
      <Text>GivePasswordScreen</Text>
    </View>
  )
}

export default GivePasswordScreen

const styles = StyleSheet.create({})